# Kekkonen example app

This app is built with [Vite](https://vitejs.dev/).

- Make sure you have built Kekkonen in the parent `dist/` directory.
- Install dependencies with npm or yarn, then run the `dev` script.
